{
  "outputDirectory": "dist",
  "buildCommand": "pnpm run build",
  "devCommand": "pnpm run start:prod",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
